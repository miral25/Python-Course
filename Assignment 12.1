'''
Scraping Numbers from HTML using BeautifulSoup. 
The program will use urllib to read the HTML from the data files below, and parse the data,extracting numbers and 
compute the sum of the numbers in the file.
You need to adjust this code to look for span tags and pull out the text content of the span tag, 
convert them to integers and add them up to complete the assignment.
'''

from urllib.request import urlopen
from bs4 import BeautifulSoup
import ssl

# Ignore SSL certificate errors
ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE

url = input('Enter - ')
html = urlopen(url, context=ctx).read()
soup = BeautifulSoup(html, "html.parser")

sSum = 0

# Retrieve all of the anchor tags
tags = soup('span')
for tag in tags:
    sSum = sSum + int(tag.contents[0])  
    
    
print ("Count = ",len(tags))
print("Sum: ",sSum)

# http://py4e-data.dr-chuck.net/comments_422404.html

# Output
'''
Enter - http://py4e-data.dr-chuck.net/comments_422404.html
Count =  50
Sum:  2246
'''
